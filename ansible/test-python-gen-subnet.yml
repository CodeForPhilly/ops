#!/usr/bin/env ansible-playbook
---
- hosts: localhost
  vars:
    node_systemdb_path  : /tmp/system.db
    new_subnet_pfxlen   : 96
    new_subnet_supernet : "{{ node_domains['node']['network'] }}"
  tasks:

    - import_tasks: tasks/node-load-domains.yml
    - import_tasks: tasks/python-gen-subnet.yml

    - debug:
        var: new_subnet
